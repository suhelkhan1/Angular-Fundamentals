import { ModelTriggerDirective } from './model-trigger.directive';

describe('ModelTriggerDirective', () => {
  it('should create an instance', () => {
    const directive = new ModelTriggerDirective();
    expect(directive).toBeTruthy();
  });
});
